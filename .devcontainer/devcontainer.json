{
  "name": "DuckSimulator DevContainer",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",

  "features": {
    "ghcr.io/devcontainers/features/dotnet:2": {
      "version": "10.0"
    }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libfontconfig1 libfreetype6 && dotnet --info",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-dotnettools.csharp"
      ]
    }
  }
}

